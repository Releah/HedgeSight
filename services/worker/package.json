{
  "name": "@hedgesight/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hedgesight/contracts": "workspace:*",
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/ssh2": "^1.15.5",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
